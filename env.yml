name: elec
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - lightgbm          # conda-forge build includes libomp on macOS
  - pip
  - pip:
      - -r requirements.txt
      - torch
      - torchvision
